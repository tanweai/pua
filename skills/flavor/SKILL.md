---
name: "flavor"
description: "Explicit command-style bridge for `/pua:flavor`. Use only when the user explicitly invokes `$pua:flavor` or asks for /pua:flavor."
license: MIT
---

# flavor

This skill is the Codex bridge for the Claude-style command `/pua:flavor`.

The behavior below is generated from `commands/flavor.md`. When it references other `/pua:...` commands, use the corresponding same-suffix skills under the `pua:` namespace.

<!-- AUTO-GENERATED: run `node scripts/generate-command-alias-skills.mjs` -->

读取 `../pua/references/flavors.md` 并让用户选择切换味道。
