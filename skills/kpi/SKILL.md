---
name: "kpi"
description: "Explicit command-style bridge for `/pua:kpi`. Use only when the user explicitly invokes `$pua:kpi` or asks for /pua:kpi."
license: MIT
---

# kpi

This skill is the Codex bridge for the Claude-style command `/pua:kpi`.

The behavior below is generated from `commands/kpi.md`. When it references other `/pua:...` commands, use the corresponding same-suffix skills under the `pua:` namespace.

<!-- AUTO-GENERATED: run `node scripts/generate-command-alias-skills.mjs` -->

Invoke the pro skill and follow it exactly as presented to you. After loading, immediately generate the KPI report card.
