---
description: "PUA 切换味道 — 从 16 种味道中选择，包括阿里/字节/华为/腾讯/Netflix/Musk/Jobs/Microsoft/NVIDIA老黄/钉内钉外。"
argument-hint: "[alibaba|bytedance|huawei|tencent|nvidia|ding|...]"
---

读取 `references/flavors.md` 并让用户选择切换味道。支持 `nvidia` / `NVIDIA` / `英伟达` / `老黄味` 和 `ding` / `钉味` / `置身钉外` / `置身钉内`，写入 `~/.pua/config.json` 时保留其他字段。
