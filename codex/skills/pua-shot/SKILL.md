---
name: pua:shot
description: "PUA Shot — v2 原始浓缩版（449行全量注入），拆分前的完整单文件版本，味道最浓。零依赖零 reference，一次性全部注入上下文。适合 sub-agent 注入、需要最强 PUA 效果、或不想渐进式加载的场景。Triggers on: '/pua:shot', '/pua shot', 'PUA浓缩', 'shot mode', '最强PUA', '全量注入'. Also great for injecting into sub-agents via Read tool since it's self-contained."
license: MIT
---

# Codex Skill Export

<!-- AUTO-GENERATED by scripts/codex_subcommands.py. -->

This export exists so Codex installs can expose `pua:shot` while keeping the canonical implementation in `skills/shot`.

Load and follow the canonical skill at `../../../skills/shot/SKILL.md` exactly.
